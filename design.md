# Scrub Nurse — 游戏设计文档 (design.md)

## 一句话概述
第一人称器械护士模拟。术前把散落在 back table 的器械规整到 Mayo；术中按医生需求递送器械，医生用后放入中立区托盘，玩家取回并判断归位——还要用回 Mayo、用完回 back table；收尾把一切归位才算结束。槽位只在准备阶段(教学);手术中 Mayo 完全自由:无槽、随便放、可叠加。写实风格，手术区以无菌布覆盖，无血无组织，焦点始终在器械与手。

## 灵感来源
玩家喜欢 Steam 上的 *Librarian: Tidy Up the Arcane Library!* 这类整理游戏。本项目不照抄,而是把"整理"玩法内核迁移到一个被低估的真实职业——手术室器械护士(scrub nurse)——上,并加入知识层(认器械、懂术式流程)。

## 为什么"器械护士"是优秀的整理题材
整理的爽感来源几乎全部命中,且有真实职业依据:

| 爽感来源 | 在本作中的体现 |
|---------|--------------|
| 归类归位 | 器械按用途归位到 back table 类别区与 Mayo 自定布局 |
| 序列排序 | Mayo 按手术进程顺序摆放 |
| 配对成套 | 每台手术有固定器械包 |
| 多维约束 | 两套布局(back table 类别 / Mayo 自定) + 复用判断 + 维持自定布局同时满足 |
| 取舍清理 | 术中持续归位;收尾清空一切回 back table |
| 记忆追踪 | 记住自己承诺的布局与每件的剩余使用次数 |
| 时间压力 | 医生时钟不停,归位要在递送间隙完成 |

额外的独特延伸:**递送**——整理是为了在医生伸手前把对的器械递过去,让"整理"有了应用目的,而非静态摆设。

## 核心循环

### ① 准备阶段(慢节奏 · 纯整理 · 教学)
- 6 件器械乱序散放在 back table 台面上(前一班随手堆的);准备区一屏双桌:back table 与 Mayo 并排,同一画面内直接点取,无需切视角
- Mayo 有 6 个槽位,按手术使用序号排列;这套序号布局就是玩家术中要自己维持的"记忆宫殿"
- back table 类别区此时调暗,术中第一次用完归位时才点亮
- 画面左上方显示"器械清单":序号 + 名称 + 用途 + 使用次数(纱布 ×3、组织剪 ×2)
- 持物时屏幕顶部中央弹出名称+用途卡
- 位置正确变绿,错误变红 + 器械弹簧回弹
- 全部正确 → **出现"摆放完成,开始手术"按钮 → 点击进入术中**

### ② 术中阶段(快节奏 · 应用整理成果)
**开场过场:** 点击"进入手术"→ 连续运镜,Mayo 被推到手术台边(病人左手侧),back table 落在身后;中立区托盘在 Mayo 靠医生侧边缘(右手边,医生的手从右上伸出)现身;右下角一次性提示"器械台在身后 · 按 Q"。

**医生线(玩家不可控):**
需求 → 接器械 → 使用 0.8~1.8s → 放入中立区 → 停顿 0.2~0.8s → 下一需求。
- 开场:中立区提示需玩家点"知道了"确认,医生才开始第一个需求。
- 下一需求在"医生放回中立区"之后按计时触发,**不等待护士从托盘取走器械**。
- 中立区满 3 件 → 医生手持用过的器械停住,需求序列暂停(唯一的背压阀)。
- 使用时长与需求间隙随手术进程缩短(节奏加速)。

**护士线(玩家):**
1. 递送:点 Mayo 台面对应器械 → 器械附在手上 → 移到医生手处触碰交付。
   - 正确:医生接住使用。
   - 错误:医生手拒绝(推开/收回)→ 器械弹簧回弹 → 玩家自行放回。
2. 清中立区:点托盘里的器械取回 → **判断归位**:
   - 还有剩余次数 → 放回 Mayo;
   - 用完 → 按 Q 切到 back table 视角,放入对应类别区。
   - **手术中 Mayo 自由台面(实验)**:进入手术时准备阶段摆好的布局平铺到台面成为起点,之后槽位全部隐藏——放回 Mayo 就是随手放,任意位置、允许叠加;玩家自己的摆法就是记忆系统。新术式仍从准备阶段的硬槽位教学开始。
3. 技巧:知道某件还要用的器械,可以留在中立区托盘里,下次需求直接从托盘递。

**自罚规则(不弹窗,错误 = 时间成本):**
- 把还要用的器械误放 back table:不弹回,下次需求自己切视角去拿。
- 把用完的器械留在 Mayo:不弹回,只是占地方。
- back table 放错类别区(如手术刀放进钳夹区):弹回 + 红框反馈,保留硬判定。
- 自由阶段,Mayo 不再有"放错"反馈——只有"放得不方便",找不回自己放的器械就是代价。

**提示:** 需求气泡标注使用次数("纱布 2/3"),术前清单标总次数;术中无其他常显提示。

### ③ 收尾清扫
第 9 步交付后医生线结束;玩家把 Mayo / 中立区 / 手中的器械全部归位 back table 对应类别区才算完成。收尾不计时。

### ④ 结算
星级 = 递送正确率 + 递送阶段用时 + 收尾遗留(有器械未归位扣星)。
每颗星同时计入两个进度池:**全局资历**(所有手术累计星级 → 护士 Level I/II/III → 解锁高等级手术准入)与**术式熟练度**(单术式累计星级 → 该术式 Level I/II/III → 解锁该术式自由摆台)。结算画面显示全局等级距下一级还差几星、以及本术式熟练度变化。

## MVP 范围(明确边界)

**包含:**
- 准备阶段整理(器械散放 back table → 一件件规整到 Mayo 序号槽)
- 术中阶段(递送 + 中立区取回 + 复用判断 + Mayo/back table 归位)
- 准备阶段硬槽位(教学);手术中 Mayo 自由台面(无槽、可叠加);back table 恒为类别分区
- 玩家档案进度(PlayerProfile 持久化):全局资历等级锁高等级手术准入;术式熟练度等级管槽位自由
- 收尾清扫(全部归位才结算)
- 计分结算
- 固定相机 + Q 切换 Mayo/back table 视角
- 拾起器械时显示名称与用途(知识注入)

**不包含(后续扩展):**
- 擦拭交互(取回直接归位)
- 数字快捷键递送
- 结尾清点(纱布/器械计数)
- 多术式 / 医生偏好
- 术前限时(准备阶段倒计时)
- 器械堆叠掉落物理
- WASD 行走(以视角切换代替)
- 中立区锐器安全规则(无手对手传递等)

## 三台手术渐进结构
每台手术只引入一个新概念,按日程板等级(全局资历)解锁:

| 台 | 手术 | Level | 器械 | 新概念 | 术中形态 | 解锁 |
|----|------|-------|------|--------|---------|------|
| S1 | 阑尾炎(门诊小手术) | I | 4 件 | — | 仅 Mayo:医生直接递还,放回原槽位 | 开局可玩 |
| S2 | 膝关节置换 | II | 6 件 | 中立区 | 医生放入中立区,玩家取回归位;Mayo 自由台面 | 全局 5★ |
| S3 | 开颅 | III | 9 件 | back table | 完整归位判断(复用→Mayo,用完→back table)+ 收尾清扫 | 全局 12★ |
| 展示 | 剖腹产 | IV | — | — | 仅展示,不可玩 | 永不 |

- Mayo 恒为 6 个槽位(只减不增):S1 用 4 槽、S2 用 6 槽、S3 用 6 槽
- 所有手术的摆台一样:器械散在 back table 上(背台是所有手术的摆台场地),S1/S2 全部摆入 Mayo 槽位;S3 前 6 件摆 Mayo、其余 3 件按类别归位器械台分区(术中按 Q 去取)
- S1 术中耦合节奏:下一需求等玩家放回槽位后才来;术中背台与中立区隐藏
- S2 术中解耦 + 中立区,无"用完"概念(全部回 Mayo);背台术中隐藏
- S3 术中解耦 + 中立区 + back table 归位 + 收尾;背台全程在,分区摆台时即点亮
- 功能开关在每份 procedure 数据里(neutral_zone / back_table / surgery_free_mayo),场景按开关显隐
- 复用提示(气泡"2/3")、中立区教学(确认门)按手术自然出现

## 画面与表现
- 第一人称,固定相机。准备阶段一屏双桌(back table 与 Mayo 并排);术中前面是 Mayo + 手术区,身后是 back table
- **Mayo 台在病人左手侧(稍偏左),中立区托架在 Mayo 靠医生侧边缘(右手边,医生手从右上伸出、放下器械的行程短),医生站在病人右手侧、Mayo 对面偏右**,两者隔着病人(无菌布覆盖)相对
- **back table 在玩家身后,按 Q 在 Mayo 视角 ↔ back table 视角间切换**,光标拾取/放置交互两视角一致,持械时器械跟随光标
- 准备阶段 Mayo 显示 6 个序号槽位(硬槽位);手术中无槽,器械平放台面自由摆放
- 医生需求用**语音 + 图标 + 文字**提示(底部),气泡标注使用次数
- 中立区在 HUD 显示容量(如 2/3)
- 中立区托盘非空时亮呼吸光;首件放入时提示"点击取回归位",满员时提示"取走一件医生才能继续"(均一次性)
- 无血、无组织、无内脏
- 风格写实(参考 Librarian 的写实质感),场景为干净明亮的手术室
- 器械模型 MVP 阶段先用盒体 + 不同颜色/标签代用,后续替换真实风格模型

## 设计决策记录
- **三台手术渐进**:S1 仅 Mayo(槽位归位)→ S2 加中立区(全复用)→ S3 加 back table(归位判断+收尾);每台一个概念,等级准入,剖腹产(Level IV)仅展示
- **用"开始手术"按钮替代 3-2-1 倒计时**:摆放完成自动出现按钮,点击即进入术中,更直接
- **递送改为触碰交付**:器械附在玩家手上,触碰医生手即完成,有重量感而非"飞过去"
- **错误反馈用手而非表情**:只露手,故用"手拒绝/推开"表达不接受;配器械弹簧回弹
- **跳过擦拭**:取回直接归位,聚焦整理动作
- **复用序列 9 步(纱布 ×3、组织剪 ×2)**:Mayo / back table 归位判断有意义的前提
- **需求解耦**:下一需求在医生放回中立区后触发,不等护士取回;中立区容量 3 是唯一背压阀
- **取回经由中立区**:医生用完放入中立区托架,玩家从托盘点取,替代直接手对手取回
- **归位规则**:复用中 → Mayo;用完 → back table 类别区;误放不弹回、以时间成本自罚(back table 放错类别除外,保留弹回)
- **槽位只在准备阶段(实验)**:槽位是摆台教学;进入手术即隐藏,Mayo 变自由台面(随手放、可叠加),"记住自己承诺的布局"代替"记住系统规定的槽位"
- **自由靠星级挣出来(已简化)**:星级同时计入全局资历与术式熟练度两个池,决定手术准入;槽位自由已改为无条件(手术中恒自由),不再由熟练度门控
- **back table 类别分区**:5 个类别区,区内自由,放错类别弹回;守住整理底线且贴近现实清点
- **医生手双位置**:等待器械时在 Mayo 前端,放回时滑到中立台前端;缩回=使用
- **双层进度**:全局资历等级(所有手术累计星级,阈值试玩调)管手术准入——日程板手术行自带难度层级(Level I/II/III),Level III 手术需全局 Level III 护士;术式熟练度保留为档案数据(槽位自由已改无条件,暂不门控玩法)
- **准备阶段恒为硬槽位**:整理爽感主场,自由只发生在术中
- **复用提示**:仅在需求气泡标注"2/3",术前清单标总次数
- **收尾归位才结算**:第 9 步后清扫全部归位 back table;收尾不计时
- **结算后回日程板**:结算卡片的按钮是"回到日程板"——黑场切回 intro 重新选手术(下一台或刷星),不做原地重开;intro 支持重访(徽章已在位)
- **固定相机 + Q 切换视角**代替行走,保持精确点击手感
- **推车连续运镜**:点"进入手术"后无黑场——Mayo 相机从准备机位出发,一路推进到手术位(位置/俯角/FOV 三轨 1.1s),Mayo 同步滚到台边;背景图世界化(手术区后方固定墙),运镜时视差正确
- **准备一屏双桌**:准备阶段 back table 与 Mayo 并排同画面,Q 教学统一挪到术中一次性提示
- **back table 归位区先隐藏**:类别区初始完全隐藏(dim_alpha=0),术中第一次成功归位时才点亮
- **中立区教学**:现身时提示用途("医生用完会放进中立区,及时取走清台"),点"知道了"确认后医生才开始需求;非空呼吸光(永久示能)+ 首次放入一次性提示 + 满员后果驱动提示(容量规则不提前教)
- **槽位为场景内节点,白框即场景内容**:Highlight 白框直接放在 table_slot 场景里,编辑器里可见用于拖拽对齐,游戏启动时隐藏,持械时由放置反馈点亮
- **back table 分区为场景内节点**:5 个类别区直接放在 main 场景里(拖拽定位),代码不再生成
- **静态节点放场景,代码只生成动态内容**:语音播放器等单例节点直接在场景里建;代码生成只留给器械随机堆、SFX 池这类动态/复用的东西
- **准备阶段单一整理**:器械乱序散放在 back table,玩家一件件取起规整到 Mayo;back table 类别区留到术中归位时自然习得
